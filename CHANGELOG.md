# 变更记录

这里记录已合并版本线的用户可见变化。是否已经可下载安装，以对应版本的发布说明和实际用户渠道为准。

## 0.10.8-alpha - 2026-08-19

- 应用设置中的 GitHub 更新检查支持读取 Windows 系统代理，并明确显示检查时间、下载进度和网络/签名错误；无法更新时不会把失败提示成成功。
- 首次准备完成后会记录初始化状态，普通升级不会重复要求准备环境或强制重看说明。新用户仍会先完成连接环境保护，再进入工作台。
- “使用说明”扩展为初始化配置、服务商、安全与恢复、活动记录、实验室和整体功能六个章节；可从状态栏或当前工作区随时打开，进度只保存在本机浏览器存储。
- README 和七张无凭据离线示例截图已更新，覆盖首次准备、服务商工作台、章节目录、安全恢复、活动记录、实验室与应用设置。

## 0.10.7-alpha - 2026-08-19

- 新安装的用户先进入独立的“准备连接环境”流程：明确说明将要保护的范围，创建恢复点并选择要管理的 Codex 配置层；完成后才进入正常工作台和逐步使用说明，避免因不同电脑的现有配置而直接陷入切换前检查失败。
- 服务商工作台改为左侧服务商列表、中间配置和右侧固定检查栏。保存、模型选择、运行可用性测试与检查并切换不再需要在多个页面之间来回跳转；右侧会把连接、目录、认证、限流和响应形态分开说明。
- 默认模型现在可在输入框内搜索、展开和滚动选择；访问密钥默认以星号显示，可在本机临时显隐。服务商目录与检查结果继续不读取或展示真实密钥。
- 为兼容异常增加脱敏反馈入口：只汇总版本、接口域名、模型和失败状态；在线维护者中继尚未配置时，应用只允许导出脱敏内容，不会把资料自动上传。
- 性价比中心保留固定测试、同模型比较与手动删除记录，并通过更清晰的样本、成本、官方对照和评分布局展示结果；一次测试即可形成记录，重复测试用于提高比较稳定性。
- README、安装/排错/隐私说明、发布说明和五张无凭据截图已更新到当前工作台。GitHub alpha 为本次发布渠道；Microsoft Store 不随本次提交更新。

## 0.10.6-alpha - 2026-08-10

- 顶部四个主导航改为统一的四等分布局：窗口收窄时文字会平滑淡出并保留图标入口，不再发生两档式的突然跳位；导航状态、可访问名称和工作区不会因此改变。
- “实验室 > 性价比中心”的排名表统一了表头、数据行、列间距和对齐合同；“估算成本”和“官方对照”不再视觉粘连，最小窗口也不会产生横向滚动条。
- 性价比中心在宽屏保留可读的内容上限并居中，在 980×700 最小桌面尺寸保持完整布局；服务商、安全恢复、活动记录等工作区同步采用同一套间距、圆角和排版 token。
- README 的四张离线示例截图已按当前工作台和性价比中心界面重新生成；截图只包含隔离 fixture，不包含真实服务商、地址、密钥、账单或本机配置。

## 0.10.5-alpha - 2026-08-08

- 重新整理桌面工作台：服务商、模型目录和切换前检查成为清晰的主入口；固定的“检查并切换”位置不会因当前服务商变化而跳动，访问密钥以掩码和显隐按钮呈现。
- 每次切换前都会重新运行服务商探测；连接、响应形态、认证、计费和服务端异常会分别说明。风险可由用户确认继续，无法证明安全写入时才会阻止。
- 新增“实验室 > 性价比中心”。以同一条固定测试的实际人民币成本生成同模型排名；可先尝试从接口返回中读取本次扣费，未返回时再从服务商使用日志复制，不会切换服务商或改写 Codex 配置。
- 更新 README 的脱敏示例截图，覆盖当前服务商配置、切换前检查、配置保护和性价比中心。

## 0.10.4-alpha - 2026-08-06

- 服务商可用性测试的 Responses 探针总等待预算由 8 秒调整为有界的 15 秒，降低推理模型首次响应较慢时被误判为不可用的概率。
- 超时后仍不会自动重复提交推理请求，避免服务端已处理但响应丢失时造成重复计费；余额、限流和认证错误继续由服务端响应分别分类。
- 应用顶栏下方会明确展示当前写入 `config.toml` 的服务商，并提示已打开的 Codex 会话保留启动时连接，避免把旧会话的请求误判为新切换目标的并发或认证故障。
- 服务商列表改用手柄驱动的鼠标、触控和键盘排序，提供可访问的拖动标签与落点反馈；排序只影响切换器列表，不会切换 Codex。
- 设置中的开机启动改为明确的开关控件；确认对话框支持焦点约束和 Escape 关闭。
- 模型目录会将明确标为 embedding 或 audio 的模型标注为不适用于 Codex Responses，阻止误选；其余未知模型仍可保守地选择并接受后续可用性测试。

## 0.10.3-alpha - 2026-08-05

- 切换前会对将要写入的 config 与 auth 作为同一候选合同进行新的服务商探测；连接风险会要求确认，但不会因目录尚未加载或外部认证不可见而阻止安全写入。
- 有应用访问密钥的普通 OpenAI-compatible 中转站现在会将该密钥写入候选认证目标，并明确声明认证模式，避免“检查可用、切换后认证失败”的不一致。
- 模型目录兼容常见的 `data[]`、`models[]` 和顶层数组格式；空目录或非 JSON 响应会显示不含密钥的安全诊断，帮助定位服务端目录端点问题。
- “切换到所选服务商”成为所有工作区共用入口；风险确认框的复选框布局和键盘焦点样式已修正。
- 切换与恢复继续仅变更确认归属的 provider/auth 字段，保留 MCP、插件、项目、历史和其他未知认证字段。

## 0.10.2-alpha - 2026-08-03

- 将“应用设置”整理为顶栏入口中的启动、恢复保护和更新三组；设置不再占用工作区，也不重复服务商切换、保存或恢复操作。
- “检查更新”移入应用设置，统一展示版本、发布渠道和更新状态；本地预览、维护候选版不提供公开更新动作，稳定版和 Store 版保持各自的受信任更新路径。
- 配置保护按首次基线、自动保护、手动保存分组展示，并让自动与手动恢复点保留数量成为可理解的应用偏好。
- 修正本地 Web 诊断与稳定桌面版的更新边界，避免预览环境误打开公开下载入口。

## 0.9.0-alpha - 未发布

- 确立 GitHub 日常发布和 Microsoft Store 稳定大版本的双渠道交付。GitHub 安装包保留 Tauri updater 签名，不再因缺少 Windows PFX 阻断；Microsoft Store 包改为从已验证 GitHub tag 手动构建。
- GitHub Release 构建显式启用 stable 更新通道；Store 与维护者候选构建分别隔离为 store 和 candidate 通道。
- 维护者本机 GitHub 稳定安装与候选安装目录分别固定为 `D:\Software\Signalman AI` 和 `D:\Software\Signalman AI Candidate`；候选脚本只清理自身入口，不触碰 Store 或用户资料。
- README、安装、排错、开发与发布文档改为分别说明 GitHub 最新版与 Store 稳定版，并明确未购买 Windows 代码签名时的 SmartScreen 边界。
## 0.8.0-alpha

- 产品显示名称改为 Signalman AI；桌面窗口、安装器、MSIX 显示名、商店文案、截图和发布资产同步更新。
- 保留既有 Store ID、MSIX Identity、可执行文件名、GitHub 仓库地址与本机数据目录，确保已安装用户仍可升级并读取 DPAPI 保护的资料。
- 修复候选安装刷新时桌面进程占用导致的安装阻断。
- 仓库与文档产品化：使用者、贡献者、发布维护者和历史资料分层；新增发布就绪检查和依赖风险登记。
- 服务商可用性测试成为切换门槛：只有当前地址、模型和密钥通过真实短请求，才会写入 Codex 配置；同步模型或保存配置会使旧测试失效。
- 新增旧工具替换手册与更完整的只读交接预检，公开 README 改为面向使用者的产品首页，并使用无敏感示例截图。
- 新增 Microsoft Store MSIX 打包、身份校验和 tag 触发的构建 artifact；Store 安装版不加载 GitHub updater，更新由 Store 管理。
- GitHub setup 改为受控直装备用渠道；它不再因 Store 发布 tag 自动触发，仍要求 updater 和 Windows Authenticode 双签名。

## 0.6.0-alpha - 2026-07-18

- provider API key 与应用创建的敏感恢复副本改用当前 Windows 用户的 DPAPI 保护，并兼容迁移旧明文资料。
- 安全检查能识别当前 Codex 模型与本地目录差异；确认后只同步本地目录，不改写 Codex 配置。
- 正式 Release 同时要求更新包签名与 Windows 代码签名，并增加旧工具替换前的只读检查。

## 0.5.0-alpha - 2026-07-18

- 修正服务商可用性测试的结果分类，区分可调用、标准 Responses 形状与待确认响应。
- 将界面中的检查名称统一为“服务商可用性测试”，它不会替代真实 Codex 使用结果。

## 0.4.1-alpha - 2026-07-18

- 修复已验证更新包的安装路径，并保留受限的手动下载 fallback。
- 明确旧安装版升级到新更新基线时的一次性手动升级要求。

## 0.4.0-alpha - 2026-07-18

- 增加恢复中心、无凭据备份 manifest 和恢复确认。
- 移除旧工具特定的运行时自动导入与展示逻辑。

## 0.3.2-alpha - 2026-07-17

- 增加 Rust/Tauri 构建缓存、阶段耗时与分阶段 Release 工作流。
- 发布资产先验证再发布，已存在 Release 不会被自动覆盖。

## 0.3.1-alpha - 2026-07-16

- 修复 Windows setup 启动桌面 GUI 的入口。
- 本地 Web 后端继续作为 fallback 和诊断能力，不再作为安装后的桌面入口。

## 0.3.0-alpha - 2026-07-16

- 建立桌面安装包、签名更新、稳定安装版与本地候选包的基础流程。
- 默认保持单窗口、无托盘、无开机自启。

## 0.2.0-alpha - 2026-07-15

- 建立轻量 Tauri 桌面 GUI、provider 目录、备份与恢复基础。

## 0.1.0-alpha - 2026-07-14

- 首个公开 alpha：React/Vite 界面、Tauri/Rust 本地能力、模型目录与安全切换基础。
